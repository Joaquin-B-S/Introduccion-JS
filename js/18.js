// diferencia de metodo a una funcion

const numero1 = 20;
const numero2 = "10";

console.log(parseInt(numero2)); // Convierte el string a un número entero, es una Funcion

console.log(numero1.toString()); // Convierte el número a un string, es un Método
