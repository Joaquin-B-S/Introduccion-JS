// Arreglos

const numeros = [1, 2, 3, 4, 5];
const nombres = ["Juan", "Pedro", "Maria", "Jose"];

console.log(numeros);
console.log(numeros[0]); // Acceso al primer elemento
console.log(numeros.length); // Longitud del arreglo
